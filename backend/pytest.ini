[pytest]
pythonpath = .
asyncio_mode = strict
testpaths = tests
asyncio_default_fixture_loop_scope = function
markers =
    e2e: end-to-end tests needing a live server (opt in with: uv run pytest -m e2e)
addopts = -m "not e2e"
